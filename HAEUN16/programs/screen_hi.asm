; ============================================================================
; screen_hi.asm - STORE -> HDMI 화면 (Phase 3)
; ============================================================================
; SCREEN0 = 0x80, SCREEN1 = 0x81  ->  화면 좌상단 "HI"
; 완료: R1 = 1 (LED 6개 ON)
; ============================================================================

    JMP START

START:
    LOAD R0, 72
    STORE R0, 128
    LOAD R0, 73
    STORE R0, 129
    LOAD R1, 1
LOOP:
    JMP LOOP
